# Functions

## Topic
Functions as reusable tricks you teach the computer. Cover function declaration, parameters, return values. Show a greeting function.

## Challenge
Write a function that takes a name and returns a greeting like "Hello, Alex!"

## Success Criteria
Declares a function with at least one parameter. Uses return or console.log inside it. Calls the function at least once.
